# Copy to /etc/jadel-agent-fleet/runtime.env on the runtime host.
# Never commit real secrets.
JADEL_FLEET_HOME=/opt/jadel-agent-fleet
JADEL_SOURCES_DIR=/opt/jadel-agent-fleet/sources
ENABLE_CHATBOT=false
# OPENAI_API_KEY is required only when ENABLE_CHATBOT=true.
# OPENAI_API_KEY=
